CREATE TABLE directory ( id INT AUTO_INCREMENT PRIMARY KEY, directory_name VARCHAR(100) NOT NULL, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, location_directory_id INT DEFAULT null );
CREATE TABLE file ( id INT AUTO_INCREMENT PRIMARY KEY, filename VARCHAR(100) NOT NULL, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, last_accessed DATETIME, size INT, directory_id INT );